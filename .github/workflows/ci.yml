name: CI - Testes

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Testes automatizados
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: DESAFIO-DOIS-PONTOS-PGATS26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: DESAFIO-DOIS-PONTOS-PGATS26/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Executar testes
        run: npm test

      - name: Publicar relatorio Mochawesome
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mochawesome-report
          path: DESAFIO-DOIS-PONTOS-PGATS26/mochawesome-report/
          if-no-files-found: warn
